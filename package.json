{
  "name": "snowui",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "design-system",
    "components",
    "resource/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "generate": "turbo run generate",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@tsconfig/svelte": "^5.0.6",
    "@unocss/svelte-scoped": "^66.5.9",
    "turbo": "^2.0.0",
    "typescript": "^5.0.0",
    "unocss": "^66.5.9"
  }
}